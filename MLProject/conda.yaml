name: credit_risk_env
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12.7
  - pip
  - pip:
      - mlflow==2.19.0
      - scikit-learn
      - pandas
      - numpy
      - matplotlib
      - seaborn
      - optuna
      - dagshub
      - mlserver
      - mlserver-mlflow